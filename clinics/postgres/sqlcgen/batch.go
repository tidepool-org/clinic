// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: batch.go

package sqlcgen

import (
	"context"
	"errors"

	"github.com/jackc/pgx/v5"
	"github.com/jackc/pgx/v5/pgtype"
)

var (
	ErrBatchAlreadyClosed = errors.New("batch already closed")
)

const insertClinicAdmin = `-- name: InsertClinicAdmin :batchexec
INSERT INTO clinic_admins (clinic_id, user_id)
VALUES ($1, $2)
ON CONFLICT (clinic_id, user_id) DO NOTHING
`

type InsertClinicAdminBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

type InsertClinicAdminParams struct {
	ClinicID string
	UserID   string
}

func (q *Queries) InsertClinicAdmin(ctx context.Context, arg []InsertClinicAdminParams) *InsertClinicAdminBatchResults {
	batch := &pgx.Batch{}
	for _, a := range arg {
		vals := []interface{}{
			a.ClinicID,
			a.UserID,
		}
		batch.Queue(insertClinicAdmin, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &InsertClinicAdminBatchResults{br, len(arg), false}
}

func (b *InsertClinicAdminBatchResults) Exec(f func(int, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		if b.closed {
			if f != nil {
				f(t, ErrBatchAlreadyClosed)
			}
			continue
		}
		_, err := b.br.Exec()
		if f != nil {
			f(t, err)
		}
	}
}

func (b *InsertClinicAdminBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}

const insertClinicMembershipRestriction = `-- name: InsertClinicMembershipRestriction :batchexec
INSERT INTO clinic_membership_restrictions (clinic_id, email_domain, required_idp)
VALUES ($1, $2, $3)
ON CONFLICT (clinic_id, email_domain) DO UPDATE SET
    required_idp = EXCLUDED.required_idp
`

type InsertClinicMembershipRestrictionBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

type InsertClinicMembershipRestrictionParams struct {
	ClinicID    string
	EmailDomain string
	RequiredIdp pgtype.Text
}

func (q *Queries) InsertClinicMembershipRestriction(ctx context.Context, arg []InsertClinicMembershipRestrictionParams) *InsertClinicMembershipRestrictionBatchResults {
	batch := &pgx.Batch{}
	for _, a := range arg {
		vals := []interface{}{
			a.ClinicID,
			a.EmailDomain,
			a.RequiredIdp,
		}
		batch.Queue(insertClinicMembershipRestriction, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &InsertClinicMembershipRestrictionBatchResults{br, len(arg), false}
}

func (b *InsertClinicMembershipRestrictionBatchResults) Exec(f func(int, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		if b.closed {
			if f != nil {
				f(t, ErrBatchAlreadyClosed)
			}
			continue
		}
		_, err := b.br.Exec()
		if f != nil {
			f(t, err)
		}
	}
}

func (b *InsertClinicMembershipRestrictionBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}

const insertClinicPatientTag = `-- name: InsertClinicPatientTag :batchexec
INSERT INTO clinic_patient_tags (id, clinic_id, name)
VALUES ($1, $2, $3)
ON CONFLICT (id) DO UPDATE SET
    clinic_id = EXCLUDED.clinic_id,
    name = EXCLUDED.name
`

type InsertClinicPatientTagBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

type InsertClinicPatientTagParams struct {
	ID       string
	ClinicID string
	Name     string
}

func (q *Queries) InsertClinicPatientTag(ctx context.Context, arg []InsertClinicPatientTagParams) *InsertClinicPatientTagBatchResults {
	batch := &pgx.Batch{}
	for _, a := range arg {
		vals := []interface{}{
			a.ID,
			a.ClinicID,
			a.Name,
		}
		batch.Queue(insertClinicPatientTag, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &InsertClinicPatientTagBatchResults{br, len(arg), false}
}

func (b *InsertClinicPatientTagBatchResults) Exec(f func(int, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		if b.closed {
			if f != nil {
				f(t, ErrBatchAlreadyClosed)
			}
			continue
		}
		_, err := b.br.Exec()
		if f != nil {
			f(t, err)
		}
	}
}

func (b *InsertClinicPatientTagBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}

const insertClinicPhoneNumber = `-- name: InsertClinicPhoneNumber :batchexec
INSERT INTO clinic_phone_numbers (clinic_id, ordinal, type, number)
VALUES ($1, $2, $3, $4)
ON CONFLICT (clinic_id, ordinal) DO UPDATE SET
    type = EXCLUDED.type,
    number = EXCLUDED.number
`

type InsertClinicPhoneNumberBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

type InsertClinicPhoneNumberParams struct {
	ClinicID string
	Ordinal  int32
	Type     pgtype.Text
	Number   string
}

func (q *Queries) InsertClinicPhoneNumber(ctx context.Context, arg []InsertClinicPhoneNumberParams) *InsertClinicPhoneNumberBatchResults {
	batch := &pgx.Batch{}
	for _, a := range arg {
		vals := []interface{}{
			a.ClinicID,
			a.Ordinal,
			a.Type,
			a.Number,
		}
		batch.Queue(insertClinicPhoneNumber, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &InsertClinicPhoneNumberBatchResults{br, len(arg), false}
}

func (b *InsertClinicPhoneNumberBatchResults) Exec(f func(int, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		if b.closed {
			if f != nil {
				f(t, ErrBatchAlreadyClosed)
			}
			continue
		}
		_, err := b.br.Exec()
		if f != nil {
			f(t, err)
		}
	}
}

func (b *InsertClinicPhoneNumberBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}

const insertClinicShareCode = `-- name: InsertClinicShareCode :batchexec
INSERT INTO clinic_share_codes (share_code, clinic_id)
VALUES ($1, $2)
ON CONFLICT (share_code) DO UPDATE SET clinic_id = EXCLUDED.clinic_id
`

type InsertClinicShareCodeBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

type InsertClinicShareCodeParams struct {
	ShareCode string
	ClinicID  string
}

func (q *Queries) InsertClinicShareCode(ctx context.Context, arg []InsertClinicShareCodeParams) *InsertClinicShareCodeBatchResults {
	batch := &pgx.Batch{}
	for _, a := range arg {
		vals := []interface{}{
			a.ShareCode,
			a.ClinicID,
		}
		batch.Queue(insertClinicShareCode, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &InsertClinicShareCodeBatchResults{br, len(arg), false}
}

func (b *InsertClinicShareCodeBatchResults) Exec(f func(int, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		if b.closed {
			if f != nil {
				f(t, ErrBatchAlreadyClosed)
			}
			continue
		}
		_, err := b.br.Exec()
		if f != nil {
			f(t, err)
		}
	}
}

func (b *InsertClinicShareCodeBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}

const insertClinicSite = `-- name: InsertClinicSite :batchexec
INSERT INTO clinic_sites (id, clinic_id, name)
VALUES ($1, $2, $3)
ON CONFLICT (id) DO UPDATE SET
    clinic_id = EXCLUDED.clinic_id,
    name = EXCLUDED.name
`

type InsertClinicSiteBatchResults struct {
	br     pgx.BatchResults
	tot    int
	closed bool
}

type InsertClinicSiteParams struct {
	ID       string
	ClinicID string
	Name     string
}

func (q *Queries) InsertClinicSite(ctx context.Context, arg []InsertClinicSiteParams) *InsertClinicSiteBatchResults {
	batch := &pgx.Batch{}
	for _, a := range arg {
		vals := []interface{}{
			a.ID,
			a.ClinicID,
			a.Name,
		}
		batch.Queue(insertClinicSite, vals...)
	}
	br := q.db.SendBatch(ctx, batch)
	return &InsertClinicSiteBatchResults{br, len(arg), false}
}

func (b *InsertClinicSiteBatchResults) Exec(f func(int, error)) {
	defer b.br.Close()
	for t := 0; t < b.tot; t++ {
		if b.closed {
			if f != nil {
				f(t, ErrBatchAlreadyClosed)
			}
			continue
		}
		_, err := b.br.Exec()
		if f != nil {
			f(t, err)
		}
	}
}

func (b *InsertClinicSiteBatchResults) Close() error {
	b.closed = true
	return b.br.Close()
}
